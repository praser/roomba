{
  "name": "roomba",
  "version": "0.0.0",
  "private": true,
  "description": "roomba monorepo",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "roomba": "node apps/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
